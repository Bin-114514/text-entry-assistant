# 文字输入助手首版 SPEC

## 目标

在 Windows 11 x64 上提供一个本地 WPF 工具：用户在准备窗口中输入或粘贴文字，捕获当前前台窗口后，通过全局快捷键把文字按 Unicode 文本元素逐步注入当前焦点控件。工具不保存正文、不上传内容、不自动提交目标应用。

## 交付范围

- WPF 准备窗口：多行中文、英文、标点、emoji；正文只保存在进程内。
- `SendInput` Unicode 后端，隔离在 `ITextInjector` 接口后；返回系统输入流错误。
- 启动/暂停/继续/紧急停止热键，注册冲突显示为可理解错误。
- 发送前捕获前台 HWND、进程 ID 和窗口类名的非敏感快照；发送中目标 HWND 变化或关闭立即取消。
- 核心状态：Idle、Armed、Running、Paused、Completed、Cancelled、Failed。
- 换行和 Tab 默认在计划中产生暂停点，由 UI 选择 Enter、Shift+Enter 或取消本次任务。
- 可配置批次间隔；取消使用独立 `CancellationToken`，批次足够小以限制停止延迟。
- 自动测试覆盖 Unicode 顺序、CRLF/LF/CR、暂停点、重复启动、取消、焦点失效和部分失败。
- Windows CI、可复现 win-x64 便携包、校验和及兼容性记录。

## 明确限制

系统输入成功只代表事件进入系统输入流，不代表目标控件接收、网页编辑器状态同步或服务器保存成功。SendInput 受 UIPI 和目标应用实现影响；不提权、不改写 `isTrusted`、不静默回退剪贴板。目标窗口相同但同窗口焦点变化的检测依赖 UI Automation，信息不足时显示诊断并停止。

## 验收证据

1. 可替换注入器测试证明文本元素顺序和代理对正确。
2. Windows 原型向记事本输入合成的中文、英文、标点和 emoji，并用紧急停止验证不继续调度。
3. WPF 构建、核心测试和 win-x64 发布命令成功。
4. 兼容性表区分自动测试、桌面人工验证和未验证应用。
