# 项目协作指引：文字输入助手

## 项目目标

开发面向 Windows 的本地桌面文字输入助手：用户准备一段文字，点击其他应用的输入区域，再通过全局快捷键将文字输入到当前光标位置。降低对网页 DOM、iframe 和具体编辑器的依赖。

暂定中文名「文字输入助手」，仓库候选名 `text-entry-assistant`。这是独立通用工具，不是学校官方产品。名称可在发布前调整。

首版做桌面工具，不开发完整系统输入法。用实测决定未来是否引入 Windows TSF 输入法服务，不把 TSF 当作默认必经路线。

## 已知背景与不能重复的错误

- 原始需求来自华科课程平台的 UEditor 作答区域，其可编辑 body 位于同源 iframe 内。
- 旧用户脚本曾误把外层不可编辑 body 识别为目标，导致显示已识别却未输入。
- 不能按 `ueditor_N` 的数字推断对应答案 textarea 的位置。
- 不能用 `UE.getEditor(iframe.id)` 查找现有实例：传错 ID 可能创建新实例。
- 看见页面文字不代表编辑器状态、表单或服务器保存成功。也不能在没有证据时把失败归因于隐藏字段未同步。
- 不沿用遍历整页改样式、删除遮罩、禁用所有编辑器事件、反复注册监听等旧实现。
- 不复制用户提供的真实网页源码、账号、答案、签名字段或登录信息进入仓库。测试使用合成页面。

## 工作方式

- 默认使用中文沟通，代码命名采用清晰英文。先交付最小可用垂直切片，再扩展功能。
- 本文件提供项目方向，不代表功能已经实现或测试通过。
- 开始工作先检查现有文件、Git 状态和开发环境；保护已有修改。不在其他项目目录创建代码。
- 常规可逆实施自行推进。确实缺少发布账号、仓库归属等信息时，先完成独立工作，再询问必要信息。
- 报告中区分：实现完成、自动测试通过、真实应用人工验证通过、尚未验证。

## gstack 技能流程

用户所说的 gstar 按 gstack 理解。使用当前环境实际安装的技能，先读取相应 SKILL.md，不假装执行不存在的命令。

按阶段选择技能，不为每个小修改重复全部流程：

1. 开工：使用 `plan-eng-review` 审视输入引擎、焦点控制和测试方案，输出简短 SPEC 与 PLAN。
2. UI 设计确有需要时使用 `design-consultation`；桌面 UI 应保持简单。
3. 故障：使用 `investigate`，以复现和证据定位根因。
4. 本地网页测试夹具：使用 `browse` 或 `qa`；这些工具不代替 Windows 桌面输入验证。
5. 发布前：使用 `review` 检查变更，再使用 `ship` 完成适合本仓库的提交、推送、PR 流程。
6. 文档随实现维护，必要时使用 `document-release`。

技能依赖 Bash、浏览器守护进程或其他当前缺失工具时，记录限制并采用等价的本地验证。不得声称技能已完整运行。遵守会话授权与环境权限，不因技能模板而启用遥测或上传本地资料。

## 首版范围

目标平台为 Windows 11 x64。Windows 10 和 ARM64 在实测前不声明支持。

必须具备：

- 文本准备窗口，支持多行、中文、英文、标点和 emoji。
- 用户手动粘贴到准备窗口；如提供读取剪贴板按钮，只在点击时读取。
- 可配置全局快捷键开始、暂停、继续与紧急停止，检测快捷键注册冲突。
- 启动时捕获当前前台目标，显示进度、已发送数量与明确错误信息。
- 可配置输入间隔；用途是兼容性和用户控制，不用于宣称伪装真人。
- 默认不替换已有内容，遵循目标应用当前光标与选区行为；明确说明选中文字可能被替换。
- 目标窗口切换或关闭时停止继续发送；暂停后必须由用户显式恢复，并重新校验目标。
- 长文本输入时 UI 保持响应，停止请求不会等待整段输入完成。
- 默认不保存输入正文，不上传内容，不自动提交表单或发送聊天消息。

首版不做：完整 TSF 输入法、浏览器扩展、云同步、AI 改写、多平台支持、任意编辑器插件、自动识别并填写整张表单。

## 技术方向与验证优先级

优先评估 C# + 当前受支持的 .NET LTS + WPF + Windows 原生 API。首次开工核实 SDK 版本并记录选型，不凭空固定一个未验证的版本。用短小技术原型验证输入路径后再搭建完整 UI。

默认输入后端评估 `SendInput` 的 Unicode 文字输入能力。定义独立 `ITextInjector` 接口，避免 UI 依赖具体后端。必须理解：

- Unicode 注入、物理键盘事件、剪贴板粘贴、IME composition/commit 是不同机制。
- 系统注入不保证每个应用接受，也不保证目标网站将其视为真人输入；不承诺修改或伪造 `isTrusted`。
- UIPI 和目标进程权限等级可能阻止输入，SendInput 的错误信息不一定能区分原因。默认不提权，提供可理解的诊断。
- SendInput 发送到当前焦点，并非某个指定控件。前台 HWND 相同不代表输入控件没变。
- 有条件时用 UI Automation 辅助记录和校验焦点控件，但其信息可能缺失；说明同窗口焦点变化检测的实际覆盖范围，不能宣称绝对防误输入。
- 不强抢焦点，不自动恢复发送，不在目标校验失败后重试到另一个窗口。
- 处理热键修饰键尚未释放的情况，避免输入变成快捷键；采用可取消的等待，不进入无限循环。
- 按 Unicode 文本元素安排进度与暂停边界，正确发送 UTF-16 代理对，不能把 emoji 当作一个 UTF-16 code unit。
- 输入 API 返回成功仅说明事件进入系统输入流，不证明目标控件接收，更不证明保存成功。

不要静默回退到剪贴板粘贴。若未来增加此后端，应由用户显式选择，并处理剪贴板被其他程序修改的竞争。

## 换行与停止语义

- 不能假设所有控件中的 Enter 都是换行，聊天工具可能立即发送消息。
- 默认遇到换行暂停，提示用户选择本次采用 Enter、Shift+Enter 或取消。将模式显示在界面上，选择后仍不能保证目标应用行为。
- 识别 CRLF、LF 和 CR；Tab 默认暂停提示，避免无意切换控件。
- 停止后只保证不再调度后续批次，已提交系统的事件不能撤回。批次必须足够小，并测试最大停止延迟。
- 不实现通用“撤销上次输入”：Ctrl+Z 可能撤销用户其他操作。将撤销交给目标应用，未来仅在有明确可靠性边界时增加。

## 架构

建议目录：

```text
src/TextEntryAssistant.App/       WPF 窗口、托盘、用户设置
src/TextEntryAssistant.Core/      状态机、文本分段、输入计划
src/TextEntryAssistant.Windows/   热键、焦点、SendInput、UIA
tests/                          核心测试、Windows 集成测试
fixtures/web/                   合成的浏览器测试页面
docs/                           设计、测试记录、兼容性说明
.github/workflows/              构建、测试、发布
```

核心状态机至少包含 Idle、Armed、Running、Paused、Completed、Cancelled、Failed。每次任务有独立取消令牌，禁止重复启动并发输入。

配置与输入正文分离；日志不得包含正文、剪贴板文本、完整窗口标题或其他可能带有隐私的内容。敏感控件识别可作为尽力保护，但不声称可靠识别所有密码框。

## 测试与验收

自动测试重点：文本顺序与 Unicode、换行规划、重复启动、取消、焦点失效、目标关闭、部分发送失败与错误传播。使用可替换时钟和后端，不依赖长时间真实等待。

本地夹具应包括 textarea、contenteditable、同源 iframe 编辑器、受控组件，以及会拦截 paste 的输入区域。UEditor 集成使用许可清晰的依赖并记录版本，不连接真实课程账号做自动测试。

人工验证至少覆盖：记事本、Edge/Chrome 的普通输入框和 iframe 富文本编辑器。Word 和真实课程平台由有相应环境的用户追加验证。

每条兼容性记录包含系统、应用版本、输入后端、测试内容类型、结果和限制。分别检查：

1. 中文、英文、标点、emoji 无遗漏且顺序正确。
2. 多行模式行为符合当前目标；不能误把发送消息记作输入成功。
3. 暂停、继续、停止、切换窗口、同窗口切换焦点的行为及检测局限。
4. 页面显示、编辑器状态和本地夹具序列化数据一致。
5. 真实平台保存与刷新验证需用户明确在测试内容上操作；工具本身不自动点击提交或保存。

未完成实测的应用在兼容表标记“未验证”。CI 的非交互会话通过不等于桌面输入端到端测试通过。

## GitHub 发布

最终目标是 GitHub 开源发布。当前文件是启动指引；创建仓库时读取当前会话与 GitHub 登录状态，确认仓库归属和可见性，不猜测账号。

发布前准备：README.md、LICENSE、CHANGELOG.md、CONTRIBUTING.md、SECURITY.md、.gitignore、兼容性表、问题模板和 CI。许可证默认建议 MIT，核对依赖许可后落实，不能直接复制其他项目代码而遗漏署名。

README 应说明安装与启动、快捷键、换行策略、隐私行为、已验证范围和已知限制；不要使用“全平台通用”“100% 绕过限制”“无法检测”等宣传。

Windows CI 执行还原、构建和自动测试；产出可复现的 win-x64 便携包及校验和。代码签名未配置时如实说明，不提供关闭系统保护的指引。

先交付可运行预览版，例如 `v0.1.0-preview.1`。发布前审查待提交文件，避免上传用户粘贴内容、真实网页、个人路径和凭据。GitHub Actions 设置最小权限，发布写权限只授予发布任务。

发布操作依照当前会话的明确授权执行；仓库已获授权后不反复确认同一操作。首次从空仓库发布时根据实际分支状态使用初始提交流程，不机械要求创建不存在基线的 PR。

## 开工顺序与完成定义

1. 检查环境，读取相关 gstack 技能，输出 SPEC.md 与 PLAN.md。
2. 实现最小输入原型：向记事本发送中文、英文、emoji，并验证紧急停止。
3. 若原型存在不兼容，记录证据并调整后端，不先搭完整外壳。
4. 实现状态机、焦点监测、全局热键和最简 UI。
5. 完成自动测试、夹具验证与可用环境中的桌面实测。
6. 完成文档、打包、审查和 GitHub 发布准备，按已获授权发布。

首版完成意味着：能运行、有核心测试、有实际验证记录、有可下载构建和准确说明。若发布凭据或桌面环境缺失，明确列出未完成项并交付已有产物，不虚报完成。
